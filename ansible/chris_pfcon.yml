---
# hackathonTitle: create an Ansible Playbook to setup the podman_tcp container locally. 
# hackathonColumn: Automate podman_tcp
# hackathonLabels: ansible,yml
# TODO: define the playbook to run locally
- name: Install the ChRIS containers locally in podman with Ansible
  connection: local
  hosts: localhost
  

  # TODO: Run the chris_defaults role to load the ChRIS related variables. 
  roles:
    - chris_defaults
    # TODO: Now run the chris_podman_tcp role to install the podman_tcp container to podman. 
    - chris_podman_tcp

